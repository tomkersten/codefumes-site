- if !ie?
  !!! XML
!!! Strict
%html
  %head
    = stylesheet_link_tag 'tenplate'
    = yield :head
  %body
    #header
      .grid
        %h1
          = link_to image_tag('logo_visual.png', :alt => ''), root_path, :class => 'logo visual'
          = link_to image_tag('logo_textual.png', :alt => 'Codefumes'), root_path, :class => 'logo textual'
        %p What does your code smell like?
        .session
          = render :partial => 'layouts/session_info_and_links'
    #grid
      = render :partial => "layouts/flashes"
      = yield

  = javascript_include_tag %w(raphael jquery initializer jquery.dates grid visualizer template card)

  = yield :javascript
  :javascript
    $(
    function(){
    #{yield :onReady}
    }
    );
